"""Resume note - a committable, human-editable markdown snapshot of PMB's
current state (open goals, recent decisions, key lessons, recent activity,
project structure). Inspired by Recall's `.recall/context.md` workflow but
sourced from PMB's typed memory instead of an extractive summary, so it has
real structure (goals, decisions, lessons) instead of picked sentences.
"""
